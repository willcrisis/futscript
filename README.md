# futscript

A browser-based football-manager game. Phase 1: a 16-team league with instant
match simulation and saves stored in localStorage.

## Usage

```bash
npm install
npm run dev    # start the dev server
npm test       # run the test suite
```
